# Secrets — never commit
.env
.env.*
!.env.example

# Python
backend/.venv/
__pycache__/
*.pyc

# Node / Next
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/tsconfig.tsbuildinfo
frontend/.certs/*
!frontend/.certs/san.cnf.example

# Interactive CLI session control dirs (runtime storage rehydration reads).
# Default location is inside the project; override with VC_SESSION_STORE.
.yapcode/

# Cost log (per-connection voice + Claude session cost JSONL; for analysis,
# not source). Override path with VC_COST_LOG_PATH.
cost-log.jsonl

# Debug pipeline log (full voice<->backend<->Claude event stream JSONL; runtime
# observability, not source). Override path with VC_DEBUG_LOG_PATH.
debug-log.jsonl

# Misc
.DS_Store

# Superpowers SDD scratch (ledger, briefs, review packages)
.superpowers/
.claude/worktrees/
