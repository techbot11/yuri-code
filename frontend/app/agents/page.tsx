"use client";

// The roster: the specialists Yuri hands work to. The engine table and the
// MCP servers are now their own tabs (app/agents/layout.tsx).
import { Roster } from "@/components/Roster";

export default function Page() {
  return <Roster />;
}
